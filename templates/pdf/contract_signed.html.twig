<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contrat Signé #{{ contract.contractNumber }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            margin: 0;
            padding: 20px;
            color: #333;
        }
        .header {
            text-align: center;
            border-bottom: 3px solid #28a745;
            padding-bottom: 20px;
            margin-bottom: 30px;
        }
        .logo {
            font-size: 24px;
            font-weight: bold;
            color: #28a745;
        }
        .contract-title {
            font-size: 20px;
            font-weight: bold;
            margin: 20px 0;
            text-transform: uppercase;
        }
        .signed-status {
            background-color: #28a745;
            color: white;
            padding: 10px 20px;
            border-radius: 25px;
            display: inline-block;
            font-weight: bold;
            margin: 10px 0;
        }
        .contract-number {
            font-size: 14px;
            color: #666;
        }
        .parties {
            display: flex;
            justify-content: space-between;
            margin: 30px 0;
        }
        .party {
            width: 45%;
        }
        .party h3 {
            color: #28a745;
            border-bottom: 1px solid #28a745;
            padding-bottom: 5px;
        }
        .terms-table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }
        .terms-table th,
        .terms-table td {
            border: 1px solid #ddd;
            padding: 12px;
            text-align: left;
        }
        .terms-table th {
            background-color: #f8f9fa;
            font-weight: bold;
        }
        .amount {
            font-weight: bold;
            color: #28a745;
        }
        .section {
            margin: 25px 0;
        }
        .section h4 {
            color: #28a745;
            margin-bottom: 15px;
        }
        .conditions {
            background-color: #f8f9fa;
            padding: 15px;
            border-radius: 5px;
            margin: 15px 0;
        }
        .footer {
            margin-top: 50px;
            border-top: 2px solid #28a745;
            padding-top: 20px;
        }
        .signatures {
            display: flex;
            justify-content: space-between;
            margin-top: 30px;
        }
        .signature-box {
            width: 45%;
            border: 2px solid #28a745;
            padding: 30px;
            text-align: center;
            min-height: 120px;
            background-color: #f8fff8;
        }
        .signature-box.signed {
            background-color: #d4edda;
        }
        .signature-box h5 {
            margin-bottom: 20px;
            color: #28a745;
        }
        .digital-signature {
            margin: 15px 0;
            padding: 10px;
            border: 1px dashed #28a745;
            background-color: white;
            border-radius: 5px;
        }
        .signature-image {
            max-width: 200px;
            max-height: 60px;
            border: 1px solid #ddd;
        }
        .signature-validation {
            background-color: #d4edda;
            border: 1px solid #c3e6cb;
            color: #155724;
            padding: 15px;
            border-radius: 5px;
            margin: 20px 0;
            text-align: center;
        }
        .page-break {
            page-break-before: always;
        }
        .watermark {
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-45deg);
            font-size: 48px;
            color: rgba(40, 167, 69, 0.1);
            z-index: -1;
            font-weight: bold;
        }
        .status-badge {
            position: absolute;
            top: 20px;
            right: 20px;
            background-color: #28a745;
            color: white;
            padding: 10px 15px;
            border-radius: 25px;
            font-weight: bold;
            font-size: 14px;
        }
        @media print {
            body { margin: 0; }
            .no-print { display: none; }
        }
    </style>
</head>
<body>
    <div class="watermark">SIGNÉ</div>
    <div class="status-badge">✓ CONTRAT SIGNÉ</div>
    
    <!-- En-tête -->
    <div class="header">
        <div class="logo">EdgeLoan</div>
        <div class="contract-title">Contrat de Prêt Personnel</div>
        <div class="signed-status">✓ CONTRAT SIGNÉ ÉLECTRONIQUEMENT</div>
        <div class="contract-number">Numéro : {{ contract.contractNumber }}</div>
        <div style="margin-top: 10px; font-size: 12px; color: #666;">
            Signé le {{ signedAt|date('d/m/Y à H:i') }} - Régénéré le {{ generatedAt|date('d/m/Y à H:i') }}
        </div>
    </div>

    <!-- Validation de la signature -->
    <div class="signature-validation">
        <h5>🔒 SIGNATURE ÉLECTRONIQUE VALIDÉE</h5>
        <p>
            Ce contrat a été signé électroniquement le <strong>{{ signedAt|date('d/m/Y à H:i') }}</strong>
            par <strong>{{ user.firstName }} {{ user.lastName }}</strong>.
        </p>
        <p>
            La signature électronique a la même valeur juridique qu'une signature manuscrite 
            conformément au Règlement eIDAS et au Code civil français.
        </p>
        <p style="font-size: 12px; margin-top: 15px;">
            Hash de vérification : {{ contract.contractNumber|upper }}-{{ signedAt|date('YmdHis') }}
        </p>
    </div>

    <!-- Parties contractantes -->
    <div class="parties">
        <div class="party">
            <h3>LE PRÊTEUR</h3>
            <p>
                <strong>EdgeLoan SAS</strong><br>
                Société par Actions Simplifiée<br>
                Capital social : 500 000 €<br>
                RCS Paris : 123 456 789<br>
                SIRET : 123 456 789 00012<br>
                <br>
                Siège social :<br>
                123 Avenue de la Finance<br>
                75001 Paris, France<br>
                <br>
                Tél : +33 1 23 45 67 89<br>
                Email : contact@edgeloan.fr<br>
                Site web : www.edgeloan.fr
            </p>
        </div>
        
        <div class="party">
            <h3>L'EMPRUNTEUR</h3>
            <p>
                <strong>{{ user.firstName }} {{ user.lastName }}</strong><br>
                {% if user.dateOfBirth %}
                    Né(e) le {{ user.dateOfBirth|date('d/m/Y') }}<br>
                {% endif %}
                {% if user.nationalId %}
                    Pièce d'identité : {{ user.nationalId }}<br>
                {% endif %}
                <br>
                Adresse :<br>
                {{ user.address }}<br>
                {{ user.postalCode }} {{ user.city }}<br>
                {% if user.country %}{{ user.country }}<br>{% endif %}
                <br>
                Tél : {{ user.phoneNumber }}<br>
                Email : {{ user.email }}
            </p>
        </div>
    </div>

    <!-- Conditions financières -->
    <div class="section">
        <h4>CONDITIONS FINANCIÈRES ACCEPTÉES</h4>
        <table class="terms-table">
            <tr>
                <th>Montant du prêt</th>
                <td class="amount">{{ contract.originalAmount|number_format(2, ',', ' ') }}€</td>
            </tr>
            <tr>
                <th>Taux d'intérêt nominal annuel</th>
                <td>{{ contract.interestRate }}%</td>
            </tr>
            <tr>
                <th>TAEG (Taux Annuel Effectif Global)</th>
                <td>{{ contract.taeg }}%</td>
            </tr>
            <tr>
                <th>Durée de remboursement</th>
                <td>{{ contract.duration }} mois</td>
            </tr>
            <tr>
                <th>Montant de chaque mensualité</th>
                <td class="amount">{{ contract.monthlyPayment|number_format(2, ',', ' ') }}€</td>
            </tr>
            <tr>
                <th>Date de début</th>
                <td>{{ contract.startDate|date('d/m/Y') }}</td>
            </tr>
            <tr>
                <th>Premier prélèvement</th>
                <td>{{ contract.firstPaymentDate|date('d/m/Y') }}</td>
            </tr>
            <tr>
                <th>Dernier prélèvement</th>
                <td>{{ contract.endDate|date('d/m/Y') }}</td>
            </tr>
            <tr>
                <th>Montant total à rembourser</th>
                <td class="amount">{{ (contract.monthlyPayment * contract.duration)|number_format(2, ',', ' ') }}€</td>
            </tr>
        </table>
    </div>

    <!-- Modalités acceptées -->
    <div class="section">
        <h4>MODALITÉS ACCEPTÉES</h4>
        <div class="conditions">
            <p>✓ L'emprunteur accepte les conditions de remboursement par prélèvement automatique</p>
            <p>✓ L'emprunteur a pris connaissance de son droit de rétractation de 14 jours</p>
            <p>✓ L'emprunteur accepte les conditions générales de vente</p>
            <p>✓ L'emprunteur consent au traitement de ses données personnelles</p>
        </div>
    </div>

    <div class="page-break"></div>

    <!-- Signatures -->
    <div class="footer">
        <p style="text-align: center; font-weight: bold; margin-bottom: 30px; color: #28a745;">
            ✓ SIGNATURES VALIDÉES
        </p>
        
        <div class="signatures">
            <div class="signature-box signed">
                <h5>L'EMPRUNTEUR</h5>
                <p><strong>{{ user.firstName }} {{ user.lastName }}</strong></p>
                
                <div class="digital-signature">
                    <p style="font-size: 12px; margin-bottom: 10px;">Signature électronique :</p>
                    {% if signature %}
                        <img src="{{ signature }}" alt="Signature électronique" class="signature-image">
                    {% else %}
                        <p style="font-style: italic; color: #666;">Signature électronique validée</p>
                    {% endif %}
                </div>
                
                <p style="margin-top: 15px;">
                    <strong>Signé le {{ signedAt|date('d/m/Y') }}</strong><br>
                    <strong>à {{ signedAt|date('H:i') }}</strong>
                </p>
                <p style="font-size: 12px; color: #28a745;">
                    ✓ "Bon pour accord"<br>
                    ✓ "Lu et approuvé"
                </p>
            </div>
            
            <div class="signature-box">
                <h5>LE PRÊTEUR</h5>
                <p><strong>EdgeLoan SAS</strong></p>
                <p>Représentée par son Directeur Général</p>
                
                <div class="digital-signature">
                    <p style="font-size: 12px;">Signature électronique EdgeLoan</p>
                    <div style="border: 2px solid #28a745; padding: 10px; background: white; margin: 10px 0;">
                        <strong style="color: #28a745;">EdgeLoan SAS</strong><br>
                        <small>Signature numérique certifiée</small>
                    </div>
                </div>
                
                <p style="margin-top: 15px;">
                    <strong>Validé le {{ signedAt|date('d/m/Y') }}</strong><br>
                    <strong>à Paris</strong>
                </p>
            </div>
        </div>
        
        <!-- Informations de validation -->
        <div style="margin-top: 30px; background-color: #e9f7ef; padding: 20px; border-radius: 5px;">
            <h5 style="color: #28a745; margin-bottom: 15px;">🔐 INFORMATIONS DE VALIDATION</h5>
            <div style="display: flex; justify-content: space-between; font-size: 12px;">
                <div>
                    <p><strong>Date de signature :</strong> {{ signedAt|date('d/m/Y H:i:s') }}</p>
                    <p><strong>Adresse IP :</strong> [IP masquée pour confidentialité]</p>
                    <p><strong>Navigateur :</strong> [User-Agent crypté]</p>
                </div>
                <div>
                    <p><strong>Certificat :</strong> {{ contract.contractNumber }}-{{ signedAt|date('Ymd') }}</p>
                    <p><strong>Hash de signature :</strong> {{ (contract.contractNumber ~ signedAt|date('YmdHis'))|upper|slice(0, 16) }}...</p>
                    <p><strong>Statut :</strong> <span style="color: #28a745; font-weight: bold;">✓ VALIDE</span></p>
                </div>
            </div>
        </div>
        
        <div style="margin-top: 30px; text-align: center; font-size: 12px; color: #666; border-top: 1px solid #ddd; padding-top: 15px;">
            <p>
                Ce contrat signé a été généré électroniquement par EdgeLoan le {{ generatedAt|date('d/m/Y à H:i') }}<br>
                Référence : {{ contract.contractNumber }} | Version signée<br>
                <strong style="color: #28a745;">Document légalement contraignant - Valeur juridique équivalente à un contrat papier signé</strong>
            </p>
        </div>
    </div>
</body>
</html>