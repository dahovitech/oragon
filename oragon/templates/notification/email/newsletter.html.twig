<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ subject }}</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            background-color: #f0f4f8;
            margin: 0;
            padding: 0;
        }
        .newsletter-container {
            max-width: 650px;
            margin: 20px auto;
            background-color: #ffffff;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        }
        .newsletter-header {
            background: linear-gradient(135deg, #4f46e5 0%, #7c3aed 100%);
            color: white;
            padding: 40px 30px;
            text-align: center;
        }
        .newsletter-header h1 {
            margin: 0;
            font-size: 32px;
            font-weight: 700;
            letter-spacing: -0.5px;
        }
        .newsletter-header p {
            margin: 10px 0 0 0;
            font-size: 16px;
            opacity: 0.9;
        }
        .newsletter-body {
            padding: 40px 30px;
        }
        .newsletter-title {
            font-size: 28px;
            font-weight: 700;
            color: #1a202c;
            margin-bottom: 20px;
            text-align: center;
        }
        .newsletter-content {
            font-size: 16px;
            color: #4a5568;
            line-height: 1.8;
            margin-bottom: 30px;
        }
        .newsletter-content h2 {
            color: #2d3748;
            font-size: 22px;
            margin: 30px 0 15px 0;
            border-bottom: 2px solid #e2e8f0;
            padding-bottom: 8px;
        }
        .newsletter-content h3 {
            color: #4a5568;
            font-size: 18px;
            margin: 25px 0 10px 0;
        }
        .newsletter-content ul, .newsletter-content ol {
            padding-left: 20px;
        }
        .newsletter-content li {
            margin-bottom: 8px;
        }
        .newsletter-content a {
            color: #4f46e5;
            text-decoration: none;
            font-weight: 600;
        }
        .newsletter-content a:hover {
            text-decoration: underline;
        }
        .highlight-box {
            background: linear-gradient(135deg, #fef5e7 0%, #ffefd5 100%);
            border-left: 4px solid #f6ad55;
            padding: 20px;
            margin: 25px 0;
            border-radius: 6px;
        }
        .newsletter-footer {
            background-color: #f7fafc;
            padding: 30px;
            text-align: center;
            color: #718096;
            font-size: 14px;
            border-top: 1px solid #e2e8f0;
        }
        .newsletter-footer a {
            color: #4f46e5;
            text-decoration: none;
        }
        .newsletter-footer a:hover {
            text-decoration: underline;
        }
        .social-links {
            margin: 20px 0;
        }
        .social-links a {
            display: inline-block;
            margin: 0 10px;
            color: #718096;
            text-decoration: none;
            font-weight: 600;
        }
        .divider {
            border: none;
            border-top: 2px solid #e2e8f0;
            margin: 40px 0;
        }
        .cta-section {
            text-align: center;
            margin: 40px 0;
            padding: 30px;
            background: linear-gradient(135deg, #f7fafc 0%, #edf2f7 100%);
            border-radius: 8px;
        }
        .cta-button {
            display: inline-block;
            background: linear-gradient(135deg, #4f46e5 0%, #7c3aed 100%);
            color: white;
            text-decoration: none;
            padding: 15px 30px;
            border-radius: 6px;
            font-weight: 600;
            margin: 15px 0;
            transition: transform 0.2s ease;
        }
        .cta-button:hover {
            transform: translateY(-2px);
            color: white;
        }
        @media only screen and (max-width: 650px) {
            .newsletter-container {
                border-radius: 0;
                margin: 0;
            }
            .newsletter-header, .newsletter-body, .newsletter-footer {
                padding: 20px;
            }
            .newsletter-title {
                font-size: 24px;
            }
            .newsletter-header h1 {
                font-size: 28px;
            }
            .cta-button {
                display: block;
                width: 100%;
                box-sizing: border-box;
            }
        }
    </style>
</head>
<body>
    {% if preheader %}
        <!-- Preheader text for email preview -->
        <div style="display: none; max-height: 0; overflow: hidden;">
            {{ preheader }}
        </div>
    {% endif %}
    
    <div class="newsletter-container">
        <div class="newsletter-header">
            <h1>Newsletter</h1>
            <p>{{ "now"|date("d F Y") }}</p>
        </div>
        
        <div class="newsletter-body">
            <h2 class="newsletter-title">{{ subject }}</h2>
            
            <div class="newsletter-content">
                {{ content|raw }}
            </div>
            
            <hr class="divider">
            
            <div class="cta-section">
                <h3>Restez connecté</h3>
                <p>Visitez notre site web pour plus de contenu et d'actualités.</p>
                <a href="{{ site_url|default('#') }}" class="cta-button">
                    Visiter notre site
                </a>
            </div>
        </div>
        
        <div class="newsletter-footer">
            <div class="social-links">
                <a href="#">Facebook</a>
                <a href="#">Twitter</a>
                <a href="#">LinkedIn</a>
                <a href="#">Instagram</a>
            </div>
            
            <p>
                Vous recevez cet email car vous êtes abonné à notre newsletter.
                <br>
                <a href="#">Se désabonner</a> | 
                <a href="#">Gérer mes préférences</a> | 
                <a href="#">Voir en ligne</a>
            </p>
            
            <p style="margin-top: 20px;">
                © {{ "now"|date("Y") }} {{ site_name|default('Notre Plateforme') }}. Tous droits réservés.
                <br>
                Adresse de votre entreprise, Ville, Code postal
            </p>
        </div>
    </div>
</body>
</html>